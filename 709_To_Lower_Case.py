class Solution:
    def toLowerCase(self, s: str) -> str:
        return s.lower()

# I'm embarrassed that I'm logging this entry like it's an achievement. I'm only doing this so I don't lose streak! :')
